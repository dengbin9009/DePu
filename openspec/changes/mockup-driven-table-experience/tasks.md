# 实施任务：mockup-driven-table-experience

## 1. OpenSpec 与范围确认

- [x] T001 确认本次效果图驱动范围：创建比赛、商城金币页、补充记分牌弹窗、移动牌桌、聊天/战绩/牌谱/设置面板。
- [x] T002 确认非目标入口：真实支付、好友、语音、装扮道具真实交易、SNG 完整赛事、保位离座、降落伞、投诉、收藏。
- [x] T003 对齐现有 v1/v1.1 能力，明确复用 socket、聊天、战绩榜和正式回放。

## 2. 房间配置与后端校验

- [x] T004 扩展房间创建请求和响应，支持房间名称、比赛模式、玩法、Ante、最小带入、带入上限、训练时长和单桌最大人数。
- [x] T005 为不支持或暂缓的玩法/模式返回明确错误或能力标记，避免前端误开不可用玩法。
- [x] T006 在入座买入时校验最小带入、带入上限、余额和座位冲突。
- [x] T007 在房间详情和 socket 快照中返回牌桌展示所需配置字段。
- [x] T008 编写后端测试覆盖默认配置、范围校验、买入校验和兼容旧房间。

## 3. 商城金币页

- [x] T009 新增商城页面和路由，顶部分类包含金币、钻石、VIP 卡、装扮、道具、课程。
- [x] T010 金币分类展示钱包余额、模拟充值档位、赠送文案和确认购买流程。
- [x] T011 钻石、VIP 卡、装扮、道具、课程显示占位或禁用状态，不触发未实现交易。
- [x] T012 充值成功后刷新钱包，并支持返回发起商城跳转的牌桌上下文。
- [x] T013 编写前端测试覆盖商城渲染、模拟充值成功、充值失败和返回上下文。

## 4. 创建比赛页

- [x] T014 将大厅创建房间入口升级为创建比赛页或弹层。
- [x] T015 实现训练赛/SNG 切换，训练赛可用，SNG 按能力状态处理。
- [x] T016 实现国际扑克/短牌/奥马哈切换，短牌默认可用，不可用玩法禁用或提示。
- [x] T017 实现房间名称输入和 Ante、最小带入、带入上限、训练时长、人数档位控件。
- [x] T018 提交创建后进入牌桌，并在牌桌中心展示对应配置。
- [x] T019 编写前端测试覆盖配置选择、错误提示和创建成功跳转。

## 5. 补充记分牌与入座流程

- [x] T020 牌桌空座点击打开补充记分牌弹窗，而不是直接跳转玩家列表。
- [x] T021 弹窗展示带入档位、当前带入值、消耗金币、可用金币、取消和确定。
- [x] T022 金币不足时展示前往商城入口，充值后能返回牌桌并继续买入。
- [x] T023 确认买入后调用现有入座能力，并根据服务端响应或 socket 更新刷新座位。
- [x] T024 编写测试覆盖正常买入、余额不足、座位被占、取消和商城返回。

## 6. 移动牌桌主舞台

- [x] T025 重构牌桌为移动端竖屏布局，包含顶部安全提示、网络延时、座位环、中心房间信息和底部工具栏。
- [x] T026 根据 `seatCount` 渲染 2 到 9 个座位，空座显示“空座”或“坐下”。
- [x] T027 等待态展示房主按钮：解散比赛、邀请好友、开始；非房主展示等待或邀请入口。
- [x] T028 已坐下玩家底部展示头像、昵称、状态和筹码；未坐下玩家展示坐下入口。
- [x] T029 保持现有行动按钮、倒计时、公共牌和手牌展示可用，并与新布局不重叠。
- [x] T030 编写视觉/组件测试覆盖等待态、已坐下态、房主/非房主和移动视口。

## 7. 牌桌面板

- [x] T031 实现聊天记录底部抽屉，复用现有聊天消息和 `chat.send`。
- [x] T032 实现当前战绩侧边抽屉，复用房间战绩榜并补充带入、手数、战绩展示。
- [x] T033 实现牌谱回顾面板，复用正式手牌历史和 replay steps，空状态显示“暂无任何数据”。
- [x] T034 实现牌桌设置底部菜单，包含桌面设置、站起围观、带入记分牌、比赛设置、短牌规则、保位离座、降落伞说明、退出比赛。
- [x] T035 对未实现的收藏、投诉、保位离座、降落伞、麦克风等入口使用禁用或说明状态。
- [x] T036 编写测试覆盖面板打开关闭、数据刷新、禁用入口和状态保持。

## 8. 兼容与边界

- [x] T037 保持 socket 实时同步、开局、行动、结算、钱包更新、聊天、战绩榜和回放现有行为不回退。
- [x] T038 保持独立规则测试页入口和 `/api/games/*` 行为不变。
- [x] T039 确认所有新增中文展示文案与正式产品文档一致。
- [x] T040 确认移动端主要尺寸下文本不重叠，底部工具栏不遮挡操作按钮。

## 9. 验证

- [x] T041 运行后端测试：`cd backend && go test ./... -count=1`。
- [x] T042 运行前端测试：`cd frontend && npm test -- --run`。
- [x] T043 手动使用两个账号验证创建比赛、邀请码加入、坐下买入、开局和结算。
- [x] T044 手动验证金币不足跳商城充值后返回坐下流程。
- [x] T045 手动验证聊天记录、当前战绩、牌谱回顾和牌桌设置面板。
- [x] T046 手动验证规则测试页仍可创建测试牌局、调试设牌、历史和只读回放。
