## 1. 测试基线与 Loop 约束

- [x] 1.1 为本 change 增加前端行为契约测试，覆盖牌桌安全区、关键按钮反馈、统一牌面和牌谱步骤展示，并先确认测试能捕获当前缺失或回归行为。
- [x] 1.2 为本 change 增加后端生命周期测试矩阵，覆盖结算事件顺序、自动下一手、人数不足、重复延迟任务、房主移交和无人接管关闭房间。
- [x] 1.3 建立响应式浏览器验收脚本骨架，固定 360×640、390×844、430×932、768×1024、1280×720 视口，输出元素矩形、点击命中、控制台错误和截图结果。
- [x] 1.4 建立多账号测试数据隔离与清理方案，使用 MySQL 临时 database 或唯一测试标识，避免 Loop 多轮产生的数据相互污染。

## 2. 后端房间生命周期加固

- [x] 2.1 将手牌结算后的持久化和广播顺序收敛为：归档结果与钱包事务成功、房间转 `waiting` 且清空当前手牌、广播 `hand.settled`/钱包与战绩更新、最后广播最新 `room.updated`。
- [x] 2.2 实现或加固约 2 秒后的服务端自动下一手调度，执行时必须获得房间级串行锁并重新读取房间、座位、房主和当前手牌状态。
- [x] 2.3 覆盖人数不足、房间关闭、已有新手牌和条件变化场景，确保延迟任务取消且不会创建重复手牌或广播虚假 `hand.started`。
- [x] 2.4 加固 `room.leave` 与 `DELETE /api/rooms/{roomId}/members/me` 共用的退出事务：释放座位、按钱包规则退款、删除成员关系并返回最终房间状态。
- [x] 2.5 实现房主按循环座位顺序移交给下一位仍坐下玩家，原子更新 `rooms.owner_user_id` 和 `room_members.role`；无人接管时关闭房间并清空当前手牌引用。
- [x] 2.6 在房主移交、普通成员退出和房间关闭后广播最新 `room.updated`，并验证旧房主立即失去、新房主立即获得房主 command 权限。

## 3. socket 顺序与重连加固

- [x] 3.1 设计并以向后兼容方式补充房间/手牌事件版本或单调顺序字段，覆盖 `room.snapshot`、`room.updated`、`hand.started`、`hand.updated` 和 `hand.settled`。
- [x] 3.2 在前端 socket 状态层记录当前房间版本、`handId` 与手牌序号，忽略上一手迟到事件和同一手牌的旧版本事件。
- [x] 3.3 让重连后的 `room.snapshot` 原子覆盖当前房间的 room、hand、presence、actionLog、chatMessages 和 leaderboard，清理快照中已不存在的旧状态。
- [x] 3.4 验证开局或行动 command 未收到 ACK 时不会自动重放；连接恢复后只允许用户基于最新快照主动重试。
- [x] 3.5 增加 socket 测试覆盖事件乱序、重复事件、结算与新手牌交错、断线三种生命周期状态以及多连接同用户场景。

## 4. 关键交互行为与反馈

- [x] 4.1 统一牌桌页和房间信息页的开局流程：发送 `room.start_hand`、防重复提交、使用 ACK/事件收敛，并在成功后展示当前手牌或返回牌桌。
- [x] 4.2 为非房主、人数不足、房间状态冲突、网络失败和 socket error 增加当前上下文可见反馈，禁止静默失败和本地伪造开局。
- [x] 4.3 使“邀请好友”复制包含邀请码的邀请文本；剪贴板失败时显示邀请码，并为成功和降级路径增加测试。
- [x] 4.4 审计牌桌全部可见按钮，确保每个按钮有真实行为或明确禁用/说明状态，覆盖设置、聊天、战绩、牌谱、离桌、退出比赛及占位入口。
- [x] 4.5 验证抽屉或模态框打开后收到 socket 更新不会冻结底层状态，关闭后展示最新房间和手牌。

## 5. 牌桌响应式安全区

- [x] 5.1 定义并测试顶部、桌面、中心牌区、座位环、英雄手牌、行动区和底部工具栏的共享安全区变量与稳定层级。
- [x] 5.2 调整顶部按钮为不会覆盖状态条和顶部座位的紧凑布局，并为长邀请码、房间名和玩家数量增加截断或收纳策略。
- [x] 5.3 调整座位环固定尺寸和位置，保证 2 至 9 人桌中长昵称、在线状态和筹码不溢出，也不拦截房主按钮或中心牌区点击。
- [x] 5.4 将公共牌与底池固定在独立中心层，降低或移动房间水印，保证 flop 至 river 和空牌状态均不与座位或元数据重叠。
- [x] 5.5 将英雄手牌、金额控件和行动按钮放入底部安全区上方的稳定布局，在有/无金额输入、不同合法动作数量时不遮挡底部工具栏。
- [x] 5.6 加固聊天、战绩、牌谱、设置抽屉和买入/规则模态框的层级、滚动和关闭控件，禁止背景透明层抢占前景点击。
- [x] 5.7 运行响应式布局度量测试，修复所有核心区域阻断性重叠、视口溢出和可点击命中失败。

## 6. 统一可读牌面

- [x] 6.1 提取或完善正式牌面组件/辅助函数，规范牌码到点数、花色、红黑样式和无效牌降级的映射，并增加单元测试。
- [x] 6.2 正式牌桌正面牌使用左上点数和偏右下单个大花色，英雄手牌大于公共牌，二者保持稳定宽高比和最低可读尺寸。
- [x] 6.3 提供不泄露信息的统一牌背，并验证未发牌、不可见底牌和图片资源失败时不会出现空白或隐藏牌文本。
- [x] 6.4 在最小移动视口和宽桌面视口验证 `10`、红桃、方块、黑桃和梅花的可读性、裁切和对比度。

## 7. 牌谱明细与隐私

- [x] 7.1 打开有历史的牌谱抽屉时自动加载最新一手，展示历史手牌编号、赢家、底池和公共牌摘要；空状态与加载失败状态清晰可重试。
- [x] 7.2 在牌谱抽屉展示当前 replay step 的序号、阶段、上一动作、底池、公共牌和玩家公开状态，并支持上一动作、下一动作和切换手牌。
- [x] 7.3 切换手牌或请求失败时清理不属于当前选择的旧步骤，防止旧回放内容冒充新选择。
- [x] 7.4 增加前后端回放隐私测试，确保前端只渲染当前 step 返回的底牌，服务端在公开前不返回隐藏底牌，非成员请求继续被拒绝。
- [x] 7.5 在浏览器中完成至少一手牌并验证历史自动出现、具体动作可逐步浏览、最终公开信息与数据库归档一致。

## 8. 自动化回归与独立审查

- [x] 8.1 运行 MySQL 环境后端全量测试：`cd backend && go test ./... -count=1`，并保存测试摘要。
- [x] 8.2 运行前端全量测试、类型检查和生产构建：`cd frontend && npm test -- --run && npm run typecheck && npm run build`。
- [x] 8.3 运行 `npx --yes @fission-ai/openspec validate table-playability-hardening --strict --no-interactive` 与 `git diff --check`。
- [x] 8.4 对全部未提交改动执行独立代码审查，重点检查规则权威性、事务原子性、socket 竞争、回放隐私、按钮静默失败和测试盲区；修复高优先级发现并重新验证。

## 9. 多账号生产级验收

- [x] 9.1 使用至少两个真实账号验证邀请码加入、坐下买入、房主开局、合法/非法行动、倒计时和完整结算，无需固定 HTTP 轮询即可同步。
- [x] 9.2 连续完成至少三手牌，验证结算展示、自动下一手、人数不足等待态、无重复手牌以及钱包/战绩/历史一致。
- [x] 9.3 验证普通玩家退出不改变房主、房主退出移交下一位坐下玩家、新房主可开局以及无人接管时房间关闭。
- [x] 9.4 在等待态、进行中和结算窗口分别模拟断线重连，验证权威快照恢复且未确认 command 不自动重放。
- [x] 9.5 在全部代表性视口验证等待态、行动态、金额控件、五张公共牌、九人座、买入弹窗和四类抽屉，保存截图和结构化重叠/命中结果。
- [x] 9.6 汇总自动化、API/socket、数据库和浏览器结果，生成中文生产验收报告；所有阻断问题修复并复测后才将本 change 判定完成。
