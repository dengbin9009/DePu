# table-playability-hardening 中文生产验收报告

## 验收结论

- 验收时间：2026-07-11 16:38–16:41（Asia/Shanghai）。
- 验收对象：当前未提交工作树，基线提交 `704226c7fac24d3ef73f76e1e6cc40dd6402931d`。
- 最终结论：**通过**。自动化、API/socket、真实 MySQL 数据一致性、断线重连、正式牌谱和响应式浏览器质量门全部通过，测试无 `SKIP`，未发现未关闭的阻断问题。
- 权威边界：正式牌局、钱包、历史和房间生命周期继续由 Go 后端与 MySQL 决定；浏览器验收通过 socket command/事件推进正式牌局，没有使用 HTTP action 接口，也没有恢复固定间隔 `/current-hand` 轮询。

## 验收环境

| 项目 | 版本或配置 |
| --- | --- |
| 操作系统时区 | macOS，Asia/Shanghai |
| Go | `go1.20.3 darwin/arm64` |
| Node.js | `v20.19.4` |
| npm | `10.8.2` |
| MySQL 客户端 | `8.0.44` |
| 数据库策略 | 每个后端或浏览器验收使用本机 MySQL 隔离 database，完成后自动清理 |

## 自动化质量门

| 质量门 | 实际命令 | 结果 |
| --- | --- | --- |
| 后端全量测试 | `cd backend && DEPU_TEST_RUN_ID=<run-id> go test ./... -count=1 -v` | 9 个测试包通过，132 条测试/子测试 `PASS`，0 `FAIL`，0 `SKIP` |
| 前端全量测试 | `PATH="$HOME/.nvm/versions/node/v20.19.4/bin:$PATH" npm --prefix frontend test -- --run` | 20 个测试文件、109 项测试全部通过 |
| 前端类型检查 | `PATH="$HOME/.nvm/versions/node/v20.19.4/bin:$PATH" npm --prefix frontend run typecheck` | 通过 |
| 前端生产构建 | `PATH="$HOME/.nvm/versions/node/v20.19.4/bin:$PATH" npm --prefix frontend run build` | 通过，Vite production bundle 成功生成 |
| 前端依赖锁 | `PATH="$HOME/.nvm/versions/node/v20.19.4/bin:$PATH" npm --prefix frontend ci --ignore-scripts --dry-run` | 通过；`frontend/package-lock.json` 与新增 Playwright 依赖一致，仓库根目录无误产锁文件 |
| OpenSpec 严格校验 | `PATH="$HOME/.nvm/versions/node/v20.19.4/bin:$PATH" npx --yes @fission-ai/openspec validate table-playability-hardening --strict --no-interactive` | 通过 |
| Git 差异检查 | `git diff --check` | 通过 |

原始日志：

- `.loop/results/table-playability-hardening-9.6-backend-20260711T083840Z.log`
- `.loop/results/table-playability-hardening-9.6-frontend-20260711T083924Z.log`
- `.loop/results/table-playability-hardening-9.6-spec-20260711T084138Z.log`

## API、socket 与数据库验收

执行命令：

`PATH="$HOME/.nvm/versions/node/v20.19.4/bin:$PATH" ./scripts/with-test-mysql.sh npm --prefix frontend run test:browser:table-multiplayer`

结果：

- 使用两个真实注册账号通过邀请码进入同一房间并分别入座；房主通过 `room.start_hand` 开局，两端通过 socket 收到一致权威状态。
- 倒计时从 31 秒下降到 30 秒；非当前玩家行动收到 `not_your_turn`；合法动作集合包含 6 项，正式行动通过 `room.action` command 提交。
- 连续完成 3 手不同手牌，记录 2 次服务端自动下一手；`duplicateHandIds` 为空。人数降为 1 后房间稳定回到 `waiting`，当前手牌为空，等待期间没有额外 `hand.started`。
- 钱包 API 与 MySQL 一致：房主 7000、玩家 2000；双方净输赢合计为 0。
- 房间历史、双方个人历史均为 3 手，手牌编号为 1、2、3；排行榜 API 与 MySQL 的手数及净输赢一致。
- MySQL 证据为 2 个用户、2 个成员、3 手已结算手牌、3 个不同 game、6 条参与者记录、27 条动作记录。
- 浏览器请求审计中正式行动 HTTP `POST /actions` 次数为 0；`/current-hand` 只出现在进入牌桌、显式收敛和最终核对阶段，没有固定间隔轮询。
- 结构化结果：`frontend/.artifacts/table-multiplayer-browser/2026-07-11T08-39-53.565Z/multiplayer-acceptance-report.json`。

房间生命周期中的普通玩家退出、房主循环移交、新旧房主权限切换和无人接管关闭房间，同时由本轮后端全量测试中的生命周期矩阵及 HTTP/socket 定向用例覆盖并通过。

## 断线重连与事件顺序验收

执行命令：

`PATH="$HOME/.nvm/versions/node/v20.19.4/bin:$PATH" ./scripts/with-test-mysql.sh npm --prefix frontend run test:browser:table-reconnect`

结果：

- 等待态重连恢复 `waiting` 房间、空手牌以及 presence、actionLog、chatMessages、leaderboard 快照集合。
- 进行中重连恢复相同 `handId`、`handVersion`、当前行动位和服务端合法动作。
- 结算窗口重连恢复 `waiting` 房间、已结算 hand 历史和人数不足后的单人座位状态。
- 验收主动制造未确认的 `room.start_hand` 与 `room.action`；重连后 `replayedCommands` 为空，未自动重放 command。
- 房主共经历 3 次断线并收到 4 次权威快照；两端均无 page error。
- 结构化结果：`frontend/.artifacts/table-reconnect-browser/2026-07-11T08-40-06.547Z/reconnect-acceptance-report.json`。

同一手牌旧版本、上一手迟到事件、重复事件和结算/新手牌交错由本轮前端 socket 顺序单元测试与后端 socket 全量测试覆盖并通过。

## 正式牌谱验收

执行命令：

`PATH="$HOME/.nvm/versions/node/v20.19.4/bin:$PATH" ./scripts/with-test-mysql.sh npm --prefix frontend run test:browser:table-replay`

结果：

- 浏览器完成正式手牌后自动出现历史摘要，并加载 10 个权威 replay steps。
- 回放覆盖翻牌前、翻牌、转牌、河牌和摊牌，展示上一动作、底池、公共牌和玩家公开状态。
- 最终浏览器步骤与 MySQL 归档在 handId、gameId、手牌编号、赢家、底池、公共牌、参与者、replay steps 和历史步骤上全部一致。
- 公开前步骤不补齐隐藏底牌，最终摊牌步骤只展示服务端返回的公开底牌；浏览器无错误。
- 结构化结果：`frontend/.artifacts/table-replay-browser/2026-07-11T08-40-12.468Z/replay-acceptance-report.json`。

## 响应式浏览器验收

执行命令：

`PATH="$HOME/.nvm/versions/node/v20.19.4/bin:$PATH" ./scripts/with-test-mysql.sh npm --prefix frontend run test:browser:table-responsive`

结果：

- 视口覆盖 `360×640`、`390×844`、`430×932`、`768×1024`、`1280×720`。
- 场景覆盖等待态九人座、买入弹窗、行动态金额控件、五张公共牌，以及设置、聊天、战绩、牌谱四类抽屉。
- 共 40 个视口场景，40 通过、0 失败；阻断性重叠、视口溢出、场景结构和可用控件点击命中检查全部通过。
- 截图和结构化结果：`frontend/.artifacts/table-responsive-browser/2026-07-11T08-40-15.321Z/`，报告文件为 `responsive-acceptance-report.json`。

## 阻断问题与残余说明

- 本轮没有发现新的功能、数据一致性、socket 状态或响应式阻断问题，无待复测阻断项。
- 浏览器关闭开发服务器连接时，Vite 偶发输出 `ECONNRESET`；这是验收脚本结束后代理 WebSocket 被主动关闭的清理噪声，不影响报告状态、页面错误检查或业务断言。
- 响应式结构化报告记录了开发环境静态资源 404 console 信息，但没有 page error，40 个场景全部通过，且不影响牌桌功能、布局或点击命中；因此不判定为阻断问题。

## 最终判定

`table-playability-hardening` 满足增量规格中的自动化、协议、多账号、数据库一致性、重连、牌谱和响应式浏览器质量门。当前 change 可以判定完成。
